import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const AdminDashboard = () => {
    return <Title level={2}>Admin Dashboard</Title>;
};

export default AdminDashboard;
