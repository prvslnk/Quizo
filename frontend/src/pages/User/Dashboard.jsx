import React from 'react';
import { Typography } from 'antd';
const { Title } = Typography;

const UserDashboard = () => {
    return <Title level={2}>User Dashboard</Title>;
};

export default UserDashboard;
